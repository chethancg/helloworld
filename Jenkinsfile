pipeline {
         agent any
         stages {
                 stage('stage 1 : Build') {
                 steps {
                     echo 'Build successfully'
                 }
                 }
                 stage('stage 2 : Test') {
                 steps {
                     echo 'Tested successfully'
                 }
                 }
                 stage('stage 3 : Deploy') {
                 steps {
                     echo 'Deployed successfully'
                 }
                 }
        }
  }
